'use strict';
let age = prompt('Введите цифру из диапазона от 14 до 90 включительно}:', '');

if (age >= 14 && age <= 90) {
	alert('Значение в диапазоне!');
} else {
	alert('Значение ВНЕ диапазона!');
}

